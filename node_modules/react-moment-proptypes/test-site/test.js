var momentPropTypes = require('../src/index');

var TestClass = React.createClass({
  propTypes : {
    dateThing : momentPropTypes.momentObj.isRequired,
  },

  render : function() {
    return 'test';
  },
});

ReactDOM.render(
  React.createElement(TestClass, {}, null),
  document.getElementById('container')
);
